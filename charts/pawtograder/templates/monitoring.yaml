{{- if .Values.monitoring.enabled -}}
{{- /*
Monitoring wiring. Emits Prometheus Operator CRs (ServiceMonitor) for every
component that exposes /metrics.

The postgres_exporter custom-queries ConfigMap deliberately does NOT live here —
see postgres-exporter-queries.yaml. Nothing in THIS file is hashed into the
Postgres primary's restart annotation, which is the point: adding a
ServiceMonitor here must never roll the database.

We do NOT deploy our own Prometheus or Grafana — the cluster's
kube-prometheus-stack auto-discovers ServiceMonitors in any namespace and the
Grafana sidecar picks up ConfigMaps labeled `grafana_dashboard: "1"`.

Each ServiceMonitor reuses the chart's selectorLabels so that even if a Service
gets renamed, the operator still finds the right pod set.
*/ -}}
---
{{- /*
Both of the following are gated on postgres.enabled: the sidecar they front only
exists inside the chart-managed Postgres StatefulSet. With postgres.enabled=false
(externally managed database) this Service would select no pods and the
ServiceMonitor would sit permanently "down" — masking the real exporter an
operator supplies for the external DB. validations.yaml refuses that combination
unless monitoring.externalPostgresExporter=true, and in that mode the operator
brings their own ServiceMonitor. The queries.yaml ConfigMap above is NOT gated:
it is still useful to mount into an external exporter to keep the metric names
the dashboards and alert rules expect.
*/ -}}
{{- if .Values.postgres.enabled }}
# Headless Service that fronts the postgres_exporter sidecar's :9187
# inside the postgres pod. We can't add the metrics port to the existing
# pawtograder-postgres Service because that Service is consumed as the
# StatefulSet's serviceName and its port list is what realtime/storage/
# auth/etc. dial. Carry component=postgres-exporter so the ServiceMonitor
# below can match exclusively on this Service.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "postgres-exporter") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "postgres-exporter") | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "postgres") | nindent 4 }}
  ports:
    - name: metrics
      port: 9187
      targetPort: metrics
---
# ServiceMonitor: postgres_exporter. Matches the metrics Service above
# (selector by component=postgres-exporter) so it doesn't also catch the
# main pawtograder-postgres Service — that one has no /metrics port.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "postgres") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "postgres-exporter") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "postgres-exporter") | nindent 6 }}
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
{{- end }}
---
# ServiceMonitor: storage-api. /metrics on the http port.
#
# This comment used to say the opposite -- that /metrics lives only on the admin
# app (ADMIN_PORT) or in multitenant mode, and that a single-tenant deploy on
# :5000 necessarily 404s. That is backwards, and it cost this target 24h+ of
# being DOWN in staging while the "expected" explanation sat right here. Read
# off the dist bundle in supabase/storage-api:v1.48.26:
#
#   app.js         plugins.metrics({ enabledEndpoint: !isMultitenant, ... })
#   plugins/metrics.js  if (prometheusMetricsEnabled) register(metricsEndpoint)
#                       -> if (enabledEndpoint) fastify.get("/metrics", ...)
#   config.js      prometheusMetricsEnabled = PROMETHEUS_METRICS_ENABLED === "true"
#
# So single-tenant is the mode where the MAIN app serves it; multitenant is
# where it moves to the admin app. The 404 was PROMETHEUS_METRICS_ENABLED being
# unset.
#
# But DO NOT set that flag on this image. It registers the route, and then the
# first scrape kills the pod:
#
#   "Reply was already sent, did you forget to \"return reply\" in the
#    \"/metrics\" (GET) route?"
#   Error [ERR_HTTP_HEADERS_SENT]: Cannot write headers after they are sent
#     -> uncaughtException -> PID 1 exits -> CrashLoopBackOff
#
# handleMetricsRequest writes the reply without returning it, so fastify's
# onSend chain double-writes the head. Confirmed on staging 2026-09-04: setting
# the flag crashlooped the storage tier on the ~30s scrape interval. The old
# advice ("set the toggle false") was therefore right -- only its stated
# mechanism was wrong, and a wrong mechanism is what made it look safe to
# "correct".
#
# So monitoring.serviceMonitors.storage defaults to FALSE (values.yaml carries
# the full note). Shipping the ServiceMonitor without the flag is not harmless
# either: it is a permanently-DOWN target on a 404, which is what shipped
# before, and a target that can never succeed trains everyone to ignore a DOWN
# storage row.
#
# There is a second, independent reason never to enable it on the main app: that
# port is PUBLICLY ROUTED. Kong's storage-v1 service maps /storage/v1/ to
# storage:5000/ with strip_path: true and a cors plugin only, so /metrics would
# answer at https://<api host>/storage/v1/metrics unauthenticated. Confirmed
# against staging -- that URL reaches storage-api's own router.
#
# The route worth trying for real storage metrics is the ADMIN app on ADMIN_PORT
# (5001), which registers the same handler behind a different hook chain and is
# NOT behind the Kong storage route. That needs its own container port, Service
# port and ServiceMonitor, and the admin app also exposes
# tenant/migration/s3-credential routes -- a deliberate exposure decision, not a
# monitoring.enabled side effect.
{{- if dig "serviceMonitors" "storage" false .Values.monitoring }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "storage") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "storage") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "storage") | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
{{- end }}
---
# ServiceMonitor: edge-functions.
#
# The edge RUNTIME serves no Prometheus metrics of its own — it never has. An
# earlier version of this comment claimed deno_http_* came from the runtime, and
# the edge-functions dashboard was built on that claim, which is why it rendered
# completely empty for its entire life. What actually happens:
#
#   * /metrics on the http port is demuxed like any other function path, to the
#     `metrics` user worker (supabase/functions/metrics/index.ts), which emits
#     the pawtograder_* queue and circuit-breaker series.
#   * charts/pawtograder/images/edge-functions/main.ts then APPENDS its own
#     pawtograder_edge_* series (per-function request counts, a pod-wide latency
#     histogram, eszip-cache and user-worker gauges) to that response before
#     returning it. Gated by edgeFunctions.metrics.enabled.
#
# METRICS_TOKEN is enforced by the user worker, not by the runtime and not by
# the demuxer — which is exactly why the demuxer appends only to a 200. An
# unauthorized scrape gets its 401 back with nothing added. With no token set,
# /metrics is open (metrics/index.ts: no token => no auth).
# The credential is `optional: true` so a deploy that DOESN'T set METRICS_TOKEN
# (e.g. staging, where edge /metrics is intentionally unauthed) still scrapes:
# without optional, the missing Secret key makes the Prometheus Operator reject
# this whole ServiceMonitor, silently dropping the edge target (rate-limiting
# dashboard goes dark). When the key IS present, the token is sent as required.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "edge-functions") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "functions") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "functions") | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
      authorization:
        type: Bearer
        credentials:
          name: {{ .Values.secrets.names.edgeFunctions }}
          key: METRICS_TOKEN
          optional: true
---
# ServiceMonitor: GoTrue (auth). /metrics on the dedicated metrics
# port (9100); requires GOTRUE_METRICS_EXPORTER=prometheus, set in
# auth.yaml. The main API port (9999) has no /metrics path.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "auth") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "auth") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "auth") | nindent 6 }}
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
---
# ServiceMonitor: kong (Prometheus plugin via admin port :8001).
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "kong") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "kong") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "kong") | nindent 6 }}
  endpoints:
    - port: admin-metrics
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
---
# ServiceMonitor: realtime. /metrics gated by an HS256 JWT signed with
# API_JWT_SECRET (= JWT_SECRET in this chart). Pre-minted bearer lives in
# pawtograder-jwt; Prometheus injects it as Authorization: Bearer <token>.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "realtime") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "realtime") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "realtime") | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
      authorization:
        type: Bearer
        credentials:
          name: {{ .Values.secrets.names.jwt }}
          key: REALTIME_METRICS_BEARER
{{- if .Values.supavisor.enabled }}
---
# ServiceMonitor: supavisor. /metrics gated by an HS256 JWT signed with
# SUPAVISOR_METRICS_JWT_SECRET (distinct from API_JWT_SECRET).
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "supavisor") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "supavisor") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "supavisor") | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
      authorization:
        type: Bearer
        credentials:
          name: {{ .Values.secrets.names.jwt }}
          key: SUPAVISOR_METRICS_BEARER
{{- end }}
{{- if and .Values.web.metricsLeader.enabled (dig "serviceMonitors" "metricsLeader" true .Values.monitoring) }}
---
# ServiceMonitor: the dedicated metrics-leader Deployment
# (templates/web-metrics-leader.yaml). Same endpoint and same bearer credential
# as the web ServiceMonitor below — it is the same image — but selected on
# `component: metrics-leader`, which is the ONLY thing that separates the two
# scrape targets.
#
# No metric relabeling here, deliberately. The workflow gauges are only ever
# .set() on the pod that holds METRICS_WORKFLOW_REFRESH_LEADER, and a prom-client
# labelled gauge that has never been .set() emits ZERO samples — so ordinary web
# pods contribute nothing to a sum() over web_workflow_* by construction, and a
# metricRelabelConfigs block dropping those families from the web job would be
# dead weight. Worse, the obvious `web_workflow_.*` regex would also drop
# web_workflow_metrics_refresh_errors_total, which is the one series that tells
# you the leader is broken.
#
# The `interval` deliberately need not be fast: refreshWorkflowMetrics() is
# throttled to web.metricsLeader.refreshIntervalSeconds app-side, so scraping
# faster than that just re-reads gauges that have not moved.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "metrics-leader") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "metrics-leader") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "metrics-leader") | nindent 6 }}
  endpoints:
    - port: http
      path: /api/metrics
      interval: {{ .Values.web.metricsLeader.scrapeInterval | default .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.web.metricsLeader.scrapeTimeout | default .Values.monitoring.scrapeTimeout }}
      authorization:
        type: Bearer
        credentials:
          name: {{ .Values.secrets.names.jwt }}
          key: METRICS_SCRAPE_TOKEN
{{- end }}
---
# ServiceMonitor: web (Next.js /api/metrics). Static bearer; the app does
# a constant-time string compare. See app/api/metrics/route.ts.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "pawtograder.componentName" (dict "ctx" . "component" "web") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pawtograder.componentLabels" (dict "ctx" . "component" "web") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "pawtograder.componentSelectorLabels" (dict "ctx" . "component" "web") | nindent 6 }}
  endpoints:
    - port: http
      path: /api/metrics
      interval: {{ .Values.monitoring.scrapeInterval }}
      scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}
      authorization:
        type: Bearer
        credentials:
          name: {{ .Values.secrets.names.jwt }}
          key: METRICS_SCRAPE_TOKEN
{{- end }}
