# Built bundle — produced by `npm run build`, never committed.
dist/

# Tarballs left behind by `npm pack` during release checks.
*.tgz
# Copied from the repo root by build.mjs so npm packs it; not a second source of truth.
LICENSE
